import { useState } from "react";

const AffichageElements = ({categorie}) => {
    const [categorie, setCategorie] = useState({});
    return (
        useEffect(() => {
            const fetchByCategorie = async (categorie) => {
                const resp = await fetch
            }
        })
    );
}
export default AffichageElements;