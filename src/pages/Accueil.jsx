import { useEffect } from "react";

const Accueil = () => {
    return(
        <div className="Accueil">
            <h2 className="accueil text-red-500">Accueil</h2>
        </div>
    )
};
export default Accueil;
